<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Design.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Dark.Dialog" parent="Base.Theme.MaterialComponents.Dialog">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/jetBlack</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="Dialog" parent="Base.Theme.MaterialComponents.Dialog">
        <item name="android:headerBackground">@color/colorAccent</item>
        <item name="android:calendarTextColor">@color/white</item>
        <item name="android:background">@color/jetBlack</item>
        <item name="android:dayOfWeekBackground">@color/colorAccent</item>
        <item name="android:yearListSelectorColor">@color/colorAccent</item>
        <item name="android:datePickerMode">calendar</item>    </style>


</resources>
